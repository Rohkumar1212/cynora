# URL of your separate backend server (Express/Node/etc.)
# lib/api.ts reads this value for every request.
NEXT_PUBLIC_API_URL=https://admin.sanctumchem.com
# NEXT_PUBLIC_API_URL=http://localhost:4000

# ---------------------------------------------------------------------------
# Razorpay (Pay Online at checkout)
# Get these from https://dashboard.razorpay.com/app/keys
# RAZORPAY_KEY_SECRET must NEVER be prefixed with NEXT_PUBLIC_ — it is only
# read server-side in app/api/razorpay/*.
# ---------------------------------------------------------------------------
RAZORPAY_KEY_ID=rzp_live_TKf0M06vqOuS3K
RAZORPAY_KEY_SECRET=CsNNfAUa4JYZ26tbiZ48DOKo
